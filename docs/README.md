# E-Store Documentation

## Structure

- `apps/web` — Next.js frontend
- `apps/backend` — Express + MongoDB backend
- `packages/types` — TypeScript types & Zod schemes
- `docs/` — Documentation

## Launch project

```bash
pnpm install
pnpm dev